<div>
    <div class="container">
        <div class="row">
            {{-- <div class="col-md-6">
                <img src="{{ asset('/'.$produk->gambar) }}" alt="" class="img-fluid">
            </div>
            <div class="col-md-6">
                <h2>{{ $produk->nama }}</h2>
                <h3>Rp. {{ number_format($produk->harga) }}</h3>
                <p>{{ $produk->deskripsi }}</p>
                <button wire:click="tambahKeranjang({{ $produk->id }})" class="btn btn-primary">Tambah Keranjang</button>
            </div> --}}
        </div>
    </div>
</div>
